micronaut:
  application:
    name: postManage
  security:
    authentication: bearer
    token:
      jwt:
        signatures:
          secret:
            generator:
              secret: pleaseChangeThisSecretForANewOne
netty:
  default:
    allocator:
      max-order: 3
socialMediaUploader:
  - name: instagram
    credentials:
      userName: userName
      password: password
      clientId: clientId
  - name: facebook
    credentials:
      userName: userName
      password: password
      clientId: clientId


